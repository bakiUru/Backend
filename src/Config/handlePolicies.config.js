
export const POLICIES ={
    PUBLIC: 'public',
    USER: 'user',
    ADMIN: 'admin',
}

export const handlePolicies = (policies,endpointPolicies) =>{
    //despues de obtgener el token validado, comparamos el rol del usuario y aceptamos o no
    //el ingreso al endpoint solicitado
    

}